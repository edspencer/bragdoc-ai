"use server";

// Placeholder for artifact actions
// This file will contain server actions related to artifacts
// Currently no suggestions support (deferred)

export async function getArtifactMetadata({ documentId }: { documentId: string }) {
  // Future: Add any server-side metadata fetching here
  return {};
}
