# Database connection for reading verification tokens
# Use read-only user credentials
DATABASE_URL=postgres://smoke_test_reader:password@host:5432/neondb?sslmode=require

# Application URL to test
APP_URL=https://www.bragdoc.ai

# Optional: Screenshot directory (defaults to ./screenshots)
SCREENSHOT_DIR=./screenshots

# Optional: Run in headed mode for debugging (default: true = headless)
HEADLESS=true
